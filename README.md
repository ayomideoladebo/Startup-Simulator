# Vite + React + TypeScript + Tailwind CSS

A modern web application starter built with Vite, React, TypeScript, and Tailwind CSS.

## 🚀 Features

- ⚡️ **Vite** - Next generation frontend tooling
- ⚛️ **React 18** - A JavaScript library for building user interfaces
- 🎯 **TypeScript** - Type safety and better developer experience
- 🎨 **Tailwind CSS** - A utility-first CSS framework
- 🔥 **Hot Module Replacement (HMR)** - Instant feedback on changes

## 📦 Getting Started

### Prerequisites

Make sure you have Node.js installed (version 16 or higher recommended).

### Installation

1. Open a **new** terminal or command prompt window (to ensure Node.js PATH is loaded)

2. Navigate to the project directory:
```bash
cd "c:\Users\display\Documents\New folder"
```

3. Install dependencies:
```bash
npm install
```

### Development

Start the development server:
```bash
npm run dev
```

The application will be available at `http://localhost:5173`

### Build

Create a production build:
```bash
npm run build
```

### Preview

Preview the production build:
```bash
npm run preview
```

## 📁 Project Structure

```
.
├── src/
│   ├── App.tsx           # Main App component
│   ├── main.tsx          # Application entry point
│   ├── index.css         # Global styles with Tailwind directives
│   └── vite-env.d.ts     # Vite type definitions
├── index.html            # HTML entry point
├── package.json          # Project dependencies and scripts
├── tsconfig.json         # TypeScript configuration
├── vite.config.ts        # Vite configuration
├── tailwind.config.js    # Tailwind CSS configuration
└── postcss.config.js     # PostCSS configuration
```

## 🎨 Customization

- **Tailwind CSS**: Customize the design system in `tailwind.config.js`
- **Components**: Add your React components in the `src/` directory
- **Styling**: Edit `src/index.css` for global styles

## 📝 Next Steps

1. Open a new terminal window
2. Run `npm install` to install all dependencies
3. Run `npm run dev` to start the development server
4. Start building your amazing app!

---

Built with ❤️ using Vite, React, TypeScript, and Tailwind CSS
